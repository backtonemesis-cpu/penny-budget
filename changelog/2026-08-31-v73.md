# Penny change log — v73

- Centred the native month selector vertically inside the compact mobile header.
- Prevented long month names such as September from wrapping onto two lines.
- Reduced only the month-selector text to 14px so long month/year combinations remain on one line.
- Kept the v72 header height, 44px touch targets, bottom navigation, month-selection logic, finance calculations and storage behaviour unchanged.

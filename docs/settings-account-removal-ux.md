# Settings account removal UX

The existing Remove action remains the user-facing control. Its availability is now evidence-aware:

- unused account: Remove is available;
- account referenced only by completed months: Remove is available;
- account referenced by any open/incomplete month: removal remains protected;
- completed-month evidence remains untouched after the account leaves the active reference list.

This avoids adding another visible Settings control and keeps the menu compact.

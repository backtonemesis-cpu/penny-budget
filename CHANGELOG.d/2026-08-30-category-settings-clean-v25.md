# Category Settings cleanup v25

- Replaced the always-visible category icon grid with a compact icon selector while preserving the same category icons and add-category behavior.
- Kept built-in categories as Hide/Show only.
- Kept custom-category Delete available only when the category is not referenced by existing records.
- Added an explicit confirmation before deleting an unused custom category.
- Removed the visible Change History card from normal Settings while preserving Penny's underlying audit log and deleted-record before-state for traceability.
- No finance calculations, reconciliation logic, historical records, backup data, or audit storage were removed.

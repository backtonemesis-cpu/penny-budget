{
  "name": "penny-budget",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node scripts/self-test.mjs && node scripts/final-audit-test.mjs && node scripts/month-setup-test.mjs && node scripts/overview-status-test.mjs && node scripts/month-setup-source-audit.mjs && node scripts/source-audit.mjs",
    "audit:prod": "npm audit --omit=dev --audit-level=moderate",
    "audit:all": "node scripts/dependency-audit.mjs",
    "build": "vite build",
    "check": "npm run audit:prod && npm run audit:all && npm test && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "vite": "^8.1.5"
  },
  "overrides": {
    "nanoid": "3.3.18",
    "postcss": "8.5.23"
  }
}
